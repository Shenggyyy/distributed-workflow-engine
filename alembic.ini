[alembic]
# Resolve scripts from the installed package, independent of the working directory.
script_location = workflow_engine:migrations
path_separator = os
output_encoding = utf-8
# Credentials are loaded by env.py through the application's Settings.
